const NoDataMessage = ({ message }) => {
  return (
    <div className="text-center w-full p-4 rounded-full bg-gray-50 text-gray-500 mt-4">
      <p>{message}</p>
    </div>
  );
};
export default NoDataMessage;
